/*------------------------------------------------------------------------------------------------
 *  RDMT STUDIOS 1986. All rights reserved.
 *  Licensed under the Apache 2.0 License. See LICENSE in the project root for license information.
 *-----------------------------------------------------------------------------------------------*/


/**
 * @todo
 */
// export class UCEStandartAttributeParser<Keys extends string> {
//     public parse(value: string): Map<Keys, string> {
//         const mapping = new Map<Keys, string>();
//         const buf = value.split(';');
//         for (let i = 0; i < buf.length; i++) {
//             const lbuf = buf[i].split(':', 1);
//             //@ts-ignore
//             mapping.set(lbuf[0], lbuf[1]);
//         };
//         return mapping
//     };
// };